# FEATURES — koel/dotenv-editor (fork)
Source: github.com/koel/dotenv-editor · no version tag in composer (branch-alias 2.x-dev) · MIT

Fork of jackiedo/laravel-dotenv-editor. Diffs from jackiedo: (1) Laravel/Illuminate bumped to `^11|^12|^13` (drops 5.8–10); (2) drops the runtime phpdotenv-version → parser selection machinery — deletes `ParserV1.php` and `ParserV2.php`, removes `$combatibleParserMap`, `selectCompatibleParser()`, `getDotenvPackageVersion()` from `DotenvEditor.php`, and hardcodes `ParserV3` (only phpdotenv ^5 matters on Laravel 11+); (3) adds a real test suite + `phpunit.xml` + `mago.toml` + `autoload-dev`/`require-dev` (phpunit ^11.5|^12, orchestra/testbench ^9–11); (4) everything else is pure code-style reformatting (whitespace, multi-line array/closure formatting). Net-new public API: none (it removes internal helpers; public surface unchanged). Tests: Y (`tests/` — DotenvEditorTest, DotenvReaderTest, DotenvWriterTest, FormatterTest, ParserTest). Abandoned/stale: actively maintained and the most current — targets Laravel 11/12/13, has CI-style tooling and tests; this is the canonical modern base the other forks build on.
